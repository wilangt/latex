% Require package ?
\begin{algorithm}[H]
	\caption{Title TODO}\label{alg:todo}
	\begin{algorithmic}[1]
		\Require $D$
		\Comment{Input Here}
		\State $x \gets 0^n$
		\For{$i \gets 1$ to $D$}
		\State $x \gets f(x)$ 
		\EndFor
		\State $p \gets 1^n$
		\For{$i \gets 1$ to $2D$}
			\State $p \gets f(p)$ 
			\If{$p = x$}
				\State \Return \texttt{Success}
			\EndIf
		\EndFor
		\State \Return \texttt{Fail}
	\end{algorithmic}
\end{algorithm}
