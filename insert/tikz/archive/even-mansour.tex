\begin{figure}[h]
	\centering
	\begin{tikzpicture}[scale=1]
			\tikzstyle{every node}=[transform shape];
			\tikzstyle{every node}=[node distance=1.6cm];

			% data path
			\node (XOR-1)[XOR,scale=1] {};
			\node [left of=XOR-1] (p) {$p$};
			\node (f-1) [right of=XOR-1,draw,rectangle,thick, minimum width=1.5em] {$F$};
			\path[line] (p) edge (XOR-1);
			\path[line] (XOR-1) edge (f-1);

				\node (XOR-2)[right of=f-1,XOR,scale=1] {};
			\path[line] (f-1) edge (XOR-2);
			\node (f-2) [right of=XOR-2] {};
			\path[line] (XOR-2) edge (f-2) ;
			\node [right of=XOR-2,node distance=1.6cm] {$c$};

			%% Subkeys
			\node (k-0) [above=0.6cm of XOR-1] {};
			\path[line] (k-0) node[above] {\small $k_1$} edge (XOR-1);

			\node (k-1) [above=0.6cm of XOR-2] {};
			\path[line] (k-1) node[above] {\small $k_2$} edge (XOR-2);

			%% intermediate values
			\node (x0) [right=.3cm of XOR-1] {};
			\node (x) [below=.5cm of x0] {};
			\path[dotted, line] (x) node[below] {\small $x$} edge (x0);

			\node (y0) [left=.35cm of XOR-2] {};
			\node (y) [below=.5cm of y0] {};
			\path[dotted, line] (y) node[below] {\small $y$} edge (y0);

	\end{tikzpicture}

	\caption{\Enc function of the Even-Mansour block cipher, adapted from \cite{TfC}}
  \label{fig:em}
\end{figure}

