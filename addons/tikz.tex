% To rename illustration ?

\usepackage{graphicx}
\usepackage{subcaption}

\usepackage{tikz}
\usetikzlibrary{shapes}
\usetikzlibrary{positioning,calc, arrows, bending}
\usepackage[demo]{tikzpeople} % For people

\tikzset{shape dot/.style= {shape = circle, scale = .6, fill = black}}
\tikzset{shape circle/.style= {shape = circle, scale = .6, draw}}
\tikzset{shape square/.style= {shape = rectangle, scale = .8, fill = black}}
\tikzset{shape star/.style= {shape = star, scale = .5, fill = black, star point
height = .2cm}}
\tikzset{shape reddot/.style= {shape = circle, scale = .5, fill = red}}
\tikzset{shape bluedot/.style= {shape = circle, scale = .5, fill = blue}}
\tikzset{shape vertex/.style= {shape = circle,scale=.7, draw}}

% pause for tikz (from https://tex.stackexchange.com/questions/55806/mindmap-tikzpicture-in-beamer-reveal-step-by-step)
\tikzset{
    invisible/.style={opacity=0},
    visible on/.style={alt={#1{}{invisible}}},
    alt/.code args={<#1>#2#3}{%
      \alt<#1>{\pgfkeysalso{#2}}{\pgfkeysalso{#3}} % \pgfkeysalso doesn't change the path
    },
  }
